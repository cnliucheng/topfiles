NODE_ENV=production
PORT=3000
# 生产环境必填，至少 32 字节随机串；未设置时服务将拒绝启动
JWT_SECRET=change-me-to-32-bytes-random
# 生产环境建议开启，确保会话 cookie 仅通过 HTTPS 传输
COOKIE_SECURE=true
# 仅在反向代理（Nginx 等）后面才开启，否则可被伪造 x-forwarded-for 绕过限流
TRUST_PROXY=false
STATIC_DIR=../dist
DB_PATH=./data.db
