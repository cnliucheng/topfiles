# 文件工作区侧边栏设计

登录后将侧边栏定位为“文件工作区”：顶部显示账户与文件数量，主按钮只保留高强调的“新建文件”；分享移到选中文件的更多操作中。账户设置保持中性样式，退出登录使用独立危险样式。

窄屏下侧边栏默认收起，以顶部的“文件”按钮打开覆盖式抽屉；选择文件后自动收起，避免压缩编辑器。文件行始终保留可触达的更多操作，不再依赖 hover 显示删除。

登出或会话失效时清空文件列表和当前文件，避免下次登录前短暂显示旧数据。显式保存会进入既有云端保存队列，保证不会与待提交的自动保存竞争。
